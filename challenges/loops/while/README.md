## 🧩 While Loop — String Iteration

### 🎯 Objective
Practice iterating over a string using a `while` loop and manipulating characters.

### 🛠️ Concepts used
- while loops
- String indexing
- String length with `len()`
- Conditional logic

### 💡 What the program does
- Iterates through each character of a string
- Appends an asterisk (`*`) after each character
- Preserves spaces without adding an asterisk


